from pyseq_pipeline.pre import utils

configfile: 'config.yaml'


rule fix_lighting:
    input:
        sections = utils.parse_sections(configfile['experiment_config_path'])
    output:
        temp(processed_zarr/{sections}.zarr,
             processed_zarr/{sections}.attr)
    script:
        scripts/preprocess.py
